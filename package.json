{
  "name": "task-manager-app",
  "version": "1.0.0",
  "description": "Task Manager — SCM Demo",
  "scripts": {
    "test": "jest",
    "lint:css": "stylelint \"src/**/*.css\"",
    "validate": "html-validate src/index.html"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0",
    "html-validate": "^8.0.0",
    "stylelint": "^15.0.0",
    "stylelint-config-standard": "^34.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
